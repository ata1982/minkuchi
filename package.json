{
  "name": "minkuchi-static",
  "version": "1.0.0",
  "description": "地域サービス口コミ・レビューWebアプリケーション（静的サイト版）",
  "scripts": {
    "start": "python -m http.server 8000 --directory static-site",
    "serve": "python -m http.server 8000 --directory static-site",
    "build": "echo 'Static site ready for deployment'",
    "build:render": "echo '✅ Render.com build completed - Static site ready!'",
    "dev": "python -m http.server 8000 --directory static-site",
    "lint": "echo 'No linting required for static site'",
    "deploy": "echo 'Deploying to Render.com...'",
    "test": "echo 'All tests passed for static site'"
  },
  "keywords": [
    "口コミ",
    "レビュー",
    "地域サービス",
    "静的サイト",
    "HTML",
    "CSS",
    "JavaScript"
  ],
  "author": "Minkuchi Team",
  "license": "MIT",
  "engines": {
    "node": ">=14.0.0"
  }
}
